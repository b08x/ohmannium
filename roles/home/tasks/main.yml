---

- name: importing variables
  debug:
    msg: "importing variables from ui role"

- include_vars:
    dir: "{{ lookup('env','PWD') }}/roles/ui/defaults/"
    extensions:
      - 'yml'

- include_tasks: templates.yml
  tags: ['templates']
